# 새 페이지 생성

$ARGUMENTS 경로에 새 페이지를 생성합니다.

생성 파일:
- `src/app/$ARGUMENTS/page.tsx` — 페이지 컴포넌트
- `src/app/$ARGUMENTS/loading.tsx` — 로딩 UI
- `src/app/$ARGUMENTS/error.tsx` — 에러 바운더리

규칙:
- Server Component 기본
- metadata export 포함
- 프로젝트 레이아웃 컨벤션 따름
